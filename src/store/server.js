// Utilities
import { defineStore } from 'pinia'



export const useServerStore = defineStore('serverstore', {
  state: () => ({
    conectado: false,
    
  }),
  getters: {
    
  },
  actions: {
  }
})
